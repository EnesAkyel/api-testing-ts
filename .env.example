# Base URL for the API under test
BASE_URL=http://localhost:8080/api/v1

# Request timeout in milliseconds
REQUEST_TIMEOUT_MS=30000

# Maximum acceptable response time for performance assertions (ms)
RESPONSE_TIME_THRESHOLD_MS=3000

# Auth credentials — must match the running API's AUTH_USERNAME / AUTH_PASSWORD
AUTH_USERNAME=
AUTH_PASSWORD=